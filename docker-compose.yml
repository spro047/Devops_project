services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - MONGODB_URI=${MONGODB_URI}

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
