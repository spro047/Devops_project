apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: LoadBalancer # or NodePort for Minikube
  selector:
    app: ims-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
